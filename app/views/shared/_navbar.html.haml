.navbar.navbar-inverse.navbar-fixed-top{:role => "navigation"}
  .container-fluid
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to 'solvarium.ru', root_path, class: "navbar-brand"
    %div
      .collapse.navbar-collapse{:style => "float: left;"}
        %ul.nav.navbar-nav
          - if user_signed_in?
            %li= link_to 'Лента', articles_path
            %li= link_to 'Проекты', projects_path
            %li= link_to 'Теги', tags_path
            %li= link_to 'Поиск', search_path
          - else
            %li= link_to 'Лента', articles_path
            %li= link_to 'Поиск', search_path
      .collapse.navbar-collapse{:style => "float: right;"}
        %ul.nav.navbar-nav
          - if user_signed_in?
            %li
              %a
                %span{:style => "color: yellow;"}
                  Здравствуйте, #{current_user.name}!
            /%li= link_to 'Сообщения', messages_path
            /%li= link_to 'Кабинет', user_path(current_user.id)
            %li= link_to 'Выйти', destroy_user_session_path, :method => :delete
          - else
            %li= link_to 'Войти', new_user_session_path
            %li= link_to 'Зарегистрироваться', new_user_registration_path
      / /.nav-collapse