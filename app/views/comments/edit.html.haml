%h1 Редактировать комментарий
= form_for(@comment , :url => {:action => 'update', :id => @comment.id}) do |f|
  %p
    = f.label 'Содержание:'
    %br/
    = f.text_area :content
    = f.hidden_field :article_id, value: params[:article_id]
  %p
    = f.submit 'Сохранить'
