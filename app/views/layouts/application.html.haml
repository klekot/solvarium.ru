!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{:charset => "utf-8"}/
    %meta{:content => "IE=edge", "http-equiv" => "X-UA-Compatible"}/
    %meta{:content => "width=device-width, initial-scale=1", :name => "viewport"}/
    %title Solvarium
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
    / Bootstrap
    / HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries
    / WARNING: Respond.js doesn't work if you view the page via file://
    /[if lt IE 9]
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
  %body
    .navbar.navbar-inverse.navbar-fixed-top{:role => "navigation"}
      .container-fluid
        .navbar-header
          %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          = link_to 'solvarium.ru', root_path, class: "navbar-brand"
        %div
          .collapse.navbar-collapse{:style => "float: left;"}
            %ul.nav.navbar-nav
              - if user_signed_in?
                %li= link_to 'Лента', articles_path
                %li= link_to 'Проекты', projects_path
                %li= link_to 'Теги', tags_path
                %li= link_to 'Поиск', search_path
              - else
                %li= link_to 'Лента', articles_path
                %li= link_to 'Поиск', search_path
          .collapse.navbar-collapse{:style => "float: right;"}
            %ul.nav.navbar-nav
              - if user_signed_in?
                %li
                  %a
                    %span{:style => "color: yellow;"}
                      Здравствуйте, #{current_user.name}!
                %li= link_to 'Выйти', destroy_user_session_path, :method => :delete
              - else
                %li= link_to 'Войти', new_user_session_path
                %li= link_to 'Зарегистрироваться', new_user_registration_path
          / /.nav-collapse
    - if current_user
      - if !current_user.current_project_id.nil?
        .current_project_panel
          %div
            %p.project-link
              Ваш текущий проект: #{link_to Project.find(current_user.current_project_id).title, project_path(Project.find(current_user.current_project_id).id)}  #{link_to '<span class="glyphicon glyphicon-plus-sign"></span><span style="font-family: Arial;"> Добавить запись</span>'.html_safe, new_article_path, class: "btn btn-info"}
      - elsif current_user.current_project_id.nil?
        .current_project_panel
          %div
            %p.project-link
              У Вас пока нет ни одного проекта. Создайте его, нажав на эту кнопку: #{link_to '<span class="glyphicon glyphicon-plus-sign"></span><span style="font-family: Arial;"> Добавить проект</span>'.html_safe, new_project_path, class: "btn btn-success"}
    - else
      .current_project_panel
    .container
      .row
        .col-md-2
        .col-md-8
          %br
          %br
          %br
          %br
          %br
          %br
          = yield
        .col-md-2
          /
            <p class="notice"><haml_silent></haml_silent></p>
            <p class="alert"><haml_silent></haml_silent></p>
    / /.container
    .footer
      %footer.center
        %p
          2016 #{"\xC2\xA9"} Igor Klekotnev
    / jQuery (necessary for Bootstrap's JavaScript plugins)
    %script{:src => "https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"}
    / Include all compiled plugins (below), or include individual files as needed
